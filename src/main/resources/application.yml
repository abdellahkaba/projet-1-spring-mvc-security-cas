spring:
  datasource:
    url: jdbc:postgresql://postgres-spring-mvc-1:5432/springmvc1?createDatabaseIfNotExist=true
    username: username
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect


server:
#  ssl:
#    key-store: /app/thekeystore.jks  # Le chemin vers votre fichier .jks
#    key-store-password: changeit
#    key-alias: tomcat
#    key-store-type: JKS  # Le type de keystore doit être JKS
  port: 8083



